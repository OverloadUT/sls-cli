{
  "name": "@spectra/sls",
  "version": "2.0.0",
  "description": "Spectra Listing System - semantic directory listing for AI agents in the Spectra hierarchy",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "sls": "dist/index.js"
  },
  "files": [
    "dist/**/*",
    "README.md",
    "SLS_SPEC.md"
  ],
  "scripts": {
    "prebuild": "node scripts/bundle-spec.js",
    "build": "tsc",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "node --test tests/unit/**/*.test.js",
    "test:integration": "node --test tests/integration/**/*.test.js",
    "prepublishOnly": "npm run build && npm test"
  },
  "keywords": [
    "cli",
    "ls",
    "semantic",
    "ai",
    "markdown",
    "frontmatter",
    "directory"
  ],
  "author": "Spectra",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "globby": "^14.0.0",
    "gray-matter": "^4.0.3",
    "ignore": "^5.3.0"
  },
  "devDependencies": {
    "@types/micromatch": "^4.0.10",
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
